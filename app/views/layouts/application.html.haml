!!!
%html{lang: 'en'}
  = render 'layouts/head'

  %body
    = render 'layouts/navbar'

    - if action_name == 'home'
      .nav-offset
        = render 'layouts/flash'
        = yield    
        
    - else
      #main.container-fluid.nav-offset
        = render 'layouts/flash'
        = yield

    =# render 'layouts/footer'
