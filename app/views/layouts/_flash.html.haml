- if flash.present?
  .alert-fixed.nav-anchor
    - flash.each do |level, msg|
      = content_tag :div, class: flash_class_for(level) do
        %button.close{ type: :button, data: {dismiss: :alert}, aria_label: 'Close' } &times;
        = raw msg